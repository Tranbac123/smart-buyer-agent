[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "quantumx-ai"
version = "0.1.0"
description = "QuantumX multi-agent system"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["packages"]
